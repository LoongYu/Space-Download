# Changelog

## 0.9.1 (2026-08-31)

### 站点菜单图标尺寸修复

- 修复 macOS 原生设置窗口展开站点菜单时，PNG/SVG 图标按资源原始尺寸渲染，导致菜单项高度和选中背景严重失控的问题
- 在图标交给原生 `NSMenuItem` 前按请求的逻辑尺寸预渲染；设置卡片保持 30pt，菜单项统一为 16pt
- 新增覆盖全部站点的菜单图标固有尺寸测试，并实际展开菜单完成截图验证

## 0.9.0 (2026-08-31)

### Telegram 公开单消息与多视频适配

- 新增独立 Telegram `SiteID`、adapter、设置与图标；仅支持匿名可访问的公开单条消息，不提供无实际 extractor 行为支撑的 Cookie 开关
- 支持 `t.me/频道/消息ID`、`telegram.me` 等价入口，并在执行前规范到 `https://t.me`、清理 `?single` 等查询参数及 fragment；私有群、受限频道和整频道明确拒绝
- 复用泛化多资源任务模型：公开消息中的每个视频成为独立任务；图片可识别但未实测前明确跳过、不宣称下载支持
- schema 从 v6 升至 v7，读取旧配置时仅补齐 Telegram 默认项，保留 Pornhub、YouTube、X、TikTok、抖音与 Instagram 全部配置
- 匿名真实 metadata 提供日期、标题、ID、频道及封面而不提供 `uploader`，据此采用日期-标题(ID)、关闭翻译并保存封面
- 使用内置 yt-dlp 2026.08.19 完成公开消息匿名 metadata、真实 MP4 下载与 ffprobe（H.264 848×480 + AAC，61.114921 秒），并验证封面 JPEG；公开双视频旧样例当前返回空结果，真实错误已记录
- 补齐 URL、adapter、多资源、命令、设置迁移、AppState/UI 测试，生成并目视检查 Telegram 设置页 PNG；Release app 通过严格签名校验

## 0.8.0 (2026-08-31)

### Instagram 公开帖子与多视频适配

- 新增独立 Instagram `SiteID`、adapter、设置、Cookie 会话状态和图标；仅接受手动选择的 `cookies.txt`，不读取浏览器认证，也不复用其他站点 Cookie
- 覆盖 `instagram.com/reel/CODE`、`/p/CODE` 与 `/tv/CODE` 标准链接，并在执行前定向移除分享查询参数和 fragment
- 复用并泛化 X 引入的多资源任务抽象，移除图片错误中的 X 硬编码；X 的多视频、进度、失败继续、重试和重复跳过行为保持不变
- 支持公开 Reel、单帖视频和 carousel 中的视频资源逐项下载；图片可以识别，但在未完成真实验证前明确跳过且不宣称下载支持
- schema 从 v5 升级至 v6；读取 v5 时只补齐 Instagram 默认项，并保留 Pornhub、YouTube、X、TikTok 与抖音原配置
- 真实匿名 metadata 验证了作者、日期、标题、ID 与多封面字段，据此采用作者/日期-标题(ID)、默认关闭翻译并保存最高像素封面
- 使用内置 yt-dlp 2026.08.19 完成公开 Reel、`/p/` 单视频的真实下载和 ffprobe，并验证公开三视频 carousel 的第 2 项 selector；旧 `/tv/` 样例当前返回空媒体响应，未伪称下载成功
- Swift 测试扩展至 89 项、0 失败（3 项无环境变量的 live 测试跳过）；生成并目视检查 Instagram 设置页 PNG
- 未实现 Telegram

## 0.7.0 (2026-08-31)

### 抖音公开单视频适配

- 新增独立的抖音 `SiteID`、adapter、设置、Cookie 会话状态与图标，不复用 TikTok 域名、规则或 Cookie
- 仅接受公开 `douyin.com/video/ID` 标准链接及交由 yt-dlp 安全重定向的 `v.douyin.com` 短链；未实现 Instagram 或 Telegram
- 沿用通用 metadata、实时 UI 进度、非百分比日志、5 次重试、失败继续与重复 ID 跳过核心，不改 Pornhub、YouTube、X、TikTok 专属行为
- schema 升级至 v5；v4 及更早配置自动补齐抖音默认项，并保留所有既有站点配置
- 抖音只接受用户手动选择的独立 `cookies.txt`，不读取 Chrome 或任何浏览器认证数据
- 匿名真实探测在 metadata 阶段被抖音风控阻止并明确要求 fresh cookies；未获得可信 metadata，因此采用保守的标题(ID)默认命名、关闭标题翻译，封面在运行时从真实 metadata 候选中选最高像素
- 因 metadata 未成功，未伪称真实下载或 ffprobe 成功；手动 Cookie 是合理但本次未持有 Cookie 验证的降级，详见 `native/DOUYIN_LIVE_PROBE.md`
- Swift 测试扩展至 79 项、0 失败，并生成和目视检查抖音设置页 PNG

## 0.6.0 (2026-08-31)

### TikTok 公开单视频适配

- 在现有 `SiteAdapter` 与通用单视频任务管线中新增 TikTok，不改变 Pornhub、YouTube、X 的站点专属行为
- 支持 `www.tiktok.com/@user/video/ID` 以及交由 yt-dlp 解析重定向的 `vm.tiktok.com` / `vt.tiktok.com` 安全短链入口
- 新增 TikTok 独立设置和图标；基于真实 metadata 采用作者/日期-标题(ID) 默认命名、默认关闭标题翻译并保存最高像素封面
- TikTok Cookies 仅支持用户手动选择 `cookies.txt`，不读取 Chrome 或任何浏览器认证数据
- schema 保持 v4，读取既有 v4 设置时自动补齐 TikTok 默认项，并保留 Pornhub、YouTube、X 配置
- 使用公开测试帖完成匿名 metadata、完整 10 秒下载和 ffprobe 验证；视频为 720×1280 HEVC、AAC 音频，详见 `native/TIKTOK_LIVE_PROBE.md`
- 测试扩展至 73 项、0 失败；新增 TikTok 设置页 PNG 渲染截图
- 未实现抖音、Instagram 或 Telegram

## 0.5.0 (2026-08-31)

### X 多资源帖子适配

- 新增可复用的社交帖子多资源任务抽象，媒体资源拥有稳定 ID、类型、选择序号与独立 metadata
- 首个串行适配站点为 X，仅识别 `x.com` / `twitter.com` 的单条 status 链接；未开发 TikTok、抖音、Instagram 或 Telegram
- 支持 X 单视频、animated GIF 和单帖多视频逐资源下载，保留实时进度、运行日志、失败统计及失败后继续
- 多资源按媒体 ID 去重并在下载前检查本地同 ID 文件，已存在资源跳过后继续处理剩余队列
- 图片类型已纳入通用模型以便未来扩展，但当前未完成真实验证，明确记录失败且不宣称支持
- 新增 X 独立命名、翻译、封面及手动 `cookies.txt` 设置；不读取浏览器 Cookie，也不复用 Pornhub/YouTube Cookie
- 设置 schema 升级至 v4，旧版配置自动补齐 X 默认项；Pornhub 与 YouTube 的既有流程保持不变
- 使用打包版本 yt-dlp 2026.08.19 对公开双视频 status 完成真实 metadata 探测，识别两个独立媒体 ID；详见 `native/X_LIVE_PROBE.md`
- 测试扩展至 67 项，并新增 X 设置页渲染截图

## 0.4.1 (2026-08-31)

### YouTube 标题与封面适配

- YouTube 翻译后的标题显式在 yt-dlp `pre_process` 阶段写回 metadata，确保命名模板使用翻译结果
- YouTube 封面从完整 `thumbnails` 列表中选择像素面积最大的版本，不再固定使用单个 `thumbnail` 字段
- 合并 metadata 与封面候选自身的 HTTP 请求头，封面专属请求头优先，减少 CDN 拒绝请求
- YouTube 匿名访问被平台机器人校验拦截时，输出更简洁的中文代理或手动 `cookies.txt` 处理建议
- 保持默认免 Cookies 且不读取浏览器数据；临时验证的 PO Token provider 无法绕过当前出口的播放器 API 拦截，因此未引入额外运行时依赖
- 测试扩展至 61 项，并通过真实 YouTube CDN 最高分辨率封面下载测试

## 0.4.0 (2026-08-31)

### 独立设置窗口与站点配置隔离

- 主下载窗口移除设置侧栏，下载输入、进度和运行日志使用完整窗口宽度
- 新增标准 macOS `SpaceDownload > 设置…` 菜单入口和 `⌘,` 快捷键
- 设置窗口重新设计为全局设置与站点设置两个独立页面
- 全局设置新增下载限速和 1–16 分片并发数，并接入 yt-dlp 下载参数
- 文件命名模板、标题翻译和封面开关迁移为 Pornhub、YouTube 两套独立配置
- 站点设置下拉框与标题使用本地打包的 Pornhub、YouTube 网站图标
- 配置 schema 升级至 v3，旧版媒体配置自动复制到两个站点并保留现有行为
- 测试扩展至 57 项，覆盖配置迁移、独立站点规则、下载参数和四种界面渲染状态

## 0.3.3 (2026-08-31)

### 移除浏览器认证

- 移除 YouTube Chrome 浏览器认证入口和 `--cookies-from-browser` 参数
- 应用不再读取任何浏览器会话数据
- YouTube 保留默认免认证下载，以及用户主动选择的独立 `cookies.txt` 方式

## 0.3.1 (2026-08-31)

### YouTube 无 Cookies 下载修复

- YouTube 公开内容默认继续使用 yt-dlp 官方无 Cookies 下载流程，Cookies 仅用于登录限制内容
- YouTube 命令不再继承用户级 yt-dlp 配置，避免外部播放器客户端、格式或认证参数干扰应用
- 未在界面启用代理时显式使用直连，不再静默继承 shell 或系统代理环境变量
- 移除应用对 YouTube 注入的固定浏览器 UA，交由 yt-dlp 根据当前播放器客户端自动处理
- 保留 yt-dlp 内置 EJS challenge solver，并自动发现受支持的 JavaScript 运行时
- 运行日志实时追加保存到 `~/Library/Logs/SpaceDownload/app.log`，应用重启后仍可排查失败原因
- 使用公开 YouTube 视频完成无 Cookies 的低清单流和 4K 视频加音频双流短片段真实下载验证
- 测试扩展至 54 项，新增运行日志持久化及 YouTube 直连参数回归测试

## 0.3.0 (2026-08-31)

### YouTube 与多站点架构

- 新增独立站点适配器与注册表，Pornhub 和 YouTube 使用各自的链接识别、列表展开、认证及下载参数
- 支持 YouTube 单视频、播放列表和频道下载
- YouTube 播放列表支持按视频序号筛选，频道支持视频、Shorts、直播和全部内容范围
- 新增 YouTube 独立 Cookies、字幕模式和语言、视频编码偏好、批量请求间隔设置
- 自动发现 Deno、Node.js、Bun 或 QuickJS 并交给 yt-dlp，提高 YouTube 格式解析完整性
- 左侧设置支持自动识别站点或手动切换，通用设置与各站点专属设置分层保存
- 旧版扁平设置文件自动迁移到版本化配置结构，Pornhub 现有配置保持兼容
- YouTube 列表中的精简视频 ID 会还原为标准视频链接，再逐项执行元数据解析、下载、进度统计和失败记录
- YouTube 视频 ID 纳入本地文件重复检查，已下载文件继续直接跳过
- 测试扩展至 53 项，覆盖站点隔离、链接分类、命令参数、配置迁移、YouTube 界面和播放列表完整任务流程

## 2026083101 (2026-08-31)

### 原生 SwiftUI 重构第一阶段

- 新增独立 `native/` SwiftUI macOS 客户端，不覆盖现有 Python 应用
- 完成 Pornhub 风格深色主界面、下载区、进度统计和自适应运行日志布局
- 完成可收缩设置栏，并保留固定的重新展开入口
- 使用 AppKit 原生目录选择器，移除 Web 状态与目录选择的冲突来源
- 与现有 Python 客户端共用 `user_settings.json`，可继承并自动保存普通设置
- 完成多链接输入、去重、链接格式校验和批量页码表达式解析
- 完成下载任务开始/停止状态协调层，为第二阶段接入 `yt-dlp` 做准备
- 新增 Swift 单元测试和 1600x900（16:9）原生界面渲染测试
- 新增原生 `.app` 构建与临时签名脚本

### 原生下载引擎第二阶段

- 接入真实 `yt-dlp` 子进程，支持实时输出、停止下载和强制终止
- 支持单视频、多链接和列表逐视频下载
- Pornhub 指定页码按网页 URL 展开，不再按列表序号解释
- 接入质量、格式、命名模板、代理、账号、密码和 Cookies 参数
- Pornhub 未提供用户 Cookies 时自动创建年龄验证 Cookies
- 下载和分片最多重试 5 次，分片并发为 8
- 输出完整 JSON metadata，并统计成功、失败、当前数量和总数
- 任务结束后完整打印失败标题、链接和错误原因
- 封面只在视频下载成功后保存，失败项目不会残留封面
- 接入标题中文翻译，翻译结果参与文件命名
- `.app` 打包官方独立版 `yt-dlp_macos`，保留 Homebrew/PATH 回退
- 新增黑橙风格原生应用图标
- 新增真实本地媒体下载集成测试，单元与渲染测试扩展至 30 项
- 修复 SwiftUI 点击开始后任务只切换状态、未实际执行 `yt-dlp` 的主线程调度问题
- 新增真实远程链接协调器回归测试，验证元数据解析、下载进度与停止/完成状态
- 前端进度条新增实时百分比，并持续更新下载速度和剩余时间
- 运行日志实时输出列表/视频解析、下载过程、进度、文件落盘和后处理信息
- 显式覆盖 `yt-dlp --print` 的 quiet 副作用，确保真实下载持续产生进度事件
- 修复嵌套 `DownloadTaskCoordinator` 状态未转发给 SwiftUI，导致后台下载正常但前端进度和日志不刷新的问题
- 运行日志不再重复打印下载百分比，实时进度仅保留在进度区域
- 界面日志将完整 JSON metadata 精简为标题、ID、作者、时长、清晰度、格式和来源摘要
- 完整 metadata 改为追加保存到 `~/Library/Logs/SpaceDownload/metadata.log`，供问题排查使用
- 下载前按文件名中的视频 ID 递归检查保存目录，已存在的视频直接提示并跳过，不重复下载
- 应用图标改为固定的 Icon Composer 单层资源，构建时统一生成 `Assets.car` 和兼容 `.icns`，避免系统重复套用背景
- 应用、可执行文件、安装包、DMG、日志及 CI 构建产物统一显示为 `SpaceDownload`
- 修复批量列表封面请求未继承 yt-dlp metadata 请求头导致 Pornhub CDN 返回 HTTP 403 的问题

## 2026033101 (2026-03-31)

### UI 与下载体验优化

- 🧭 恢复左侧设置栏默认显示，并优化顶部留白和主区域比例
- ↔️ 恢复左侧设置栏原生收缩/展开按钮
- 🎨 多链接输入框恢复为 Pornhub 风格，默认高度更紧凑
- 📂 保存目录增加同一行“浏览”按钮
- 📝 内置文件命名模板统一改为附带 `(id)`
- 📋 运行日志中的 `JSON metadata` 改为完整输出，不再截断
- ⚠️ 批量下载新增成功/失败数量统计，失败项目会在任务结束后输出标题、链接和错误原因
- 🖼️ 封面下载调整为仅在视频成功后执行，避免失败视频残留封面
- 🔗 支持多个单视频链接输入
- 📄 批量下载支持页码筛选输入
- 🗂️ 批量分页下载改为按 Pornhub 网页 `page=N` 解析，不再误用为视频序号
- 🚀 下载重试次数从 10 次下调到 5 次
- 🪟 去除启动后的强制刷新闪屏，并清理顶部无用区块/标题锚点
- 📐 进一步压缩顶部留白，收紧开始/停止按钮间距，并修正运行日志底部越界
- ↔️ 按 Streamlit 1.55.0 实际 DOM 修复左侧设置栏收起后无法再次展开的问题
- 🧹 移除顶部 `Deploy` 区域，恢复右侧下载区标题字号，并继续收紧日志区上下留白
- 🪄 右侧下载区改为独立标题/状态/日志间距样式，并移除“批量分页下载”下方说明文字
- 📐 继续放大主页核心下载区与应用标题，下移日志区并重新平衡主页上下边距
- 🧱 修复主页状态文字与“运行日志”标题重叠，并让日志区填充右侧剩余高度
- ↔️ 放开主页主区宽度，修复开始/停止按钮裁切与链接输入提示挤压
- ↩️ 主下载区界面恢复为更接近原始版本的简单布局，仅保留功能性改动
- 🔘 开始/停止按钮进一步收紧尺寸与间距，并重新对齐输入框高度
- 🪟 默认应用窗口尺寸调整为 2400x1600
- 🪟 默认应用窗口改为 1500x1000，保留 3:2 比例但避免首次打开过大
- 🪟 默认应用窗口改为 1600x900，切换为 16:9 比例
- 🧾 左侧“设置”标题字号放大，并与“视频质量”之间拉开段落间距
- 💾 左侧普通设置项改为自动保存到本地，下次启动默认沿用上次选择
- 🗂️ 新增 `local_notes/` 本地记录目录约定，默认不推送到 GitHub

## 2026032901 (2026-03-29)

### 初始版本

- ✨ 基于 yt-dlp 的图形界面下载工具
- 🎨 现代化深色主题界面
- 📊 实时下载进度显示
- 🖼️ 视频封面自动下载
- 🌐 代理设置支持
- 📝 自定义文件命名模板
- 🎯 多种视频质量选择 (4K, 2K, 1080p, 720p, 480p)
- 📦 多种输出格式支持 (mp4, mkv, webm, flv)
- 🚀 分片并发下载 (8线程)
- 🍪 Cookie 认证支持
