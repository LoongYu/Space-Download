name: Build Native macOS

on:
  workflow_dispatch:
    inputs:
      release_name:
        description: "Release name, for example 2026083101"
        required: true

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-14
    defaults:
      run:
        working-directory: native
    steps:
      - uses: actions/checkout@v4
      - name: Test Swift package
        run: swift test
      - name: Download standalone yt-dlp
        env:
          GH_TOKEN: ${{ github.token }}
        run: ./prepare_tools.sh
      - name: Build native app
        run: ./build_app.sh
      - name: Verify app and bundled engine
        run: |
          codesign --verify --deep --strict "dist/SpaceDownloadNative.app"
          "dist/SpaceDownloadNative.app/Contents/Resources/yt-dlp" --version
      - name: Create DMG
        run: |
          mkdir -p build/dmg
          ditto "dist/SpaceDownloadNative.app" "build/dmg/SpaceDownloadNative.app"
          hdiutil create -volname "SpaceDownload Native" \
            -srcfolder build/dmg \
            -ov -format UDZO \
            "dist/SpaceDownloadNative-${{ inputs.release_name }}-macos.dmg"
      - name: Create or update GitHub release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release view "${{ inputs.release_name }}" >/dev/null 2>&1 || \
            gh release create "${{ inputs.release_name }}" --title "${{ inputs.release_name }}" --notes "SpaceDownload release ${{ inputs.release_name }}"
          gh release upload "${{ inputs.release_name }}" \
            "dist/SpaceDownloadNative-${{ inputs.release_name }}-macos.dmg" \
            --clobber
